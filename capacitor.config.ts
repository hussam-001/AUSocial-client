import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.uni.ausocial",
  appName: "ausocial",
  webDir: "build",
  server: {
    androidScheme: "https",
  },
};

export default config;
